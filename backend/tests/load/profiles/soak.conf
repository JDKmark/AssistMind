# 浸泡 profile：稳态 45 分钟，观察内存 / 连接 / 缓存泄漏
# 用法：LOAD_SCENARIO=s7 locust -f locustfile.py --headless --config profiles/soak.conf
# 配合 Prometheus 观察：进程 RSS、DB 连接 active/waiting、Redis 连接数
--headless
-u 20
-r 2
-t 45m
--csv soak
--html soak.html
--only-summary

# 超载 profile：验证限流（429）与并发拒绝（503）是否按预期触发，是否存在排队雪崩
# 用法：LOAD_SCENARIO=s6 locust -f locustfile.py --headless --config profiles/stress.conf
--headless
-u 200
-r 20
-t 5m
--csv stress
--html stress.html
--only-summary

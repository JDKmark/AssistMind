## 计划：三件事联动 — ①项目运用 code-structuring ②全面升级 codebase-design ③spec 前置衔接

### ① 项目运用 code-structuring（AssistMind 后端）
按技能 5 步工作流，产出结构设计文档 + 落地最大缺口（dialog_manager 集中化）：

1. **structure.md 设计文档**（入库 `docs/backend-structure.md`）：
   - 模式判定：分层式（api → core → infra）+ Agent 层内图编排式（混合）
   - 模块清单：api/chat、agents/（ToolAgent/OpsSupervisor）、core/router、core/mall/entity_extractor、core/rag、core/mcp、core/dialog（**新增**）
   - 层间数据契约 + ASCII 数据流图 + 现有代码映射（六模块对应表已在技能内）
2. **dialog_manager 落地**（`backend/app/core/dialog/` 新模块）：
   - 从 `api/chat.py:75-81`（history 裁剪）、`agents/base.py:115`（_extract_query）、`rag/engine.py:222-227`（历史拼接）抽取职责，集中为 `DialogManager`：
     - `trim_history(history) -> history`（MEMORY_WINDOW 裁剪，chat.py 与 engine 共用）
     - `extract_query(messages) -> str`（base.py 迁移）
     - `format_history(history) -> str`（engine.py 历史拼接迁移，含 role 映射）
   - 消费方改调用：chat.py、base.py、engine.py 改为 import DialogManager 方法
   - 测试：`tests/unit/test_dialog_manager.py`（裁剪边界/多轮提取/格式映射）+ 既有测试无回归
   - 行为不变（纯抽取，无逻辑变更）——低风险

### ② 全面升级 codebase-design（~/.agents/skills/codebase-design/）
按探索发现的 A-F 六类问题：
1. **修复 SKILL.md 重复**（228 行 = 114×2，去重）
2. **补协作回链**：声明被 code-structuring / improve-codebase-architecture / spec 消费，明确边界（单模块形态 vs 系统分层）
3. **补模块间拓扑词汇**：明确「模块间关系（流水线/分层/图编排）归 code-structuring」，本技能聚焦单模块 depth/seam/adapter——加 Relationships 小节说明衔接
4. **负向边界**：补充「故意浅」的合法场景（薄 adapter、HTTP 转发层）+ 何时不适用 deep module 思维
5. **兑现 AI-navigable**：正文补定义（符号命名收敛、入口可 grep、为 AI 调用者降低认知成本）
6. **测试性设计挂钩依赖四分类**：SKILL.md 正文引用 DEEPENING.md 的依赖分类，说明「接口即测试面」与契约测试的关系
7. 附属文件一致性（词汇对齐、DESIGN-IT-TWICE 抽象化）

### ③ spec 前置衔接（~/.agents/skills/spec/SKILL.md）
在「### 2. 探索现状」加一条：**若需求涉及模块结构设计/职责划分（新增模块、重构分层、参考架构套用），先跑 code-structuring 产出 structure.md（模块清单/层间契约），其结论直接写入 spec 的 Why 与 What Changes**；在「### 2.5 设计门」补一句：结构方案在 code-structuring 阶段确认，spec 阶段不再重复设计。

### 验证
- dialog_manager：`pytest tests -k "not integration"` 全绿（新增 test_dialog_manager + 既有回归）
- 技能文件：结构检查（无重复、frontmatter 完整、回链存在）
- spec 衔接：用 phase7 或新任务模拟「code-structuring → spec」链路可走通

### 风险
- dialog_manager 纯抽取无行为变更，消费方签名保持兼容；engine.py 的 history 格式（用户/客服）作为契约固化
- 技能修改在用户级 ~/.agents/skills/，不影响项目代码与 CI